# include <stdio.h>

int main(void)
{
    int a = 22, b = a, c = b*6, d = 1,e;
    int x, y, z;
    x = y = z = 1;
    printf("a = %d,\nb = %d,\nc = %d,\nd = %d,\nx = %d,\ny = %d,\nz = %d.\n", a,b,c,d,x,y,z);
    return 0;
}