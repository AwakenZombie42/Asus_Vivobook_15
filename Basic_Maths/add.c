# include<stdio.h>

int main(void)
{
    int a,b;
    printf("Enter first number:\n");
    scanf("%d", &a);
    
    printf("Enter second number:\n");
    scanf("%d", &b);

    printf("Sum of numbers is: %d", a+b);// a-b for diffrence a*b for product a/b for quotient a%b for remainder
    return 0;
}